# assets/

Put these here:

- **`Rebecca-Wu-Resume.pdf`** — the "Résumé" buttons in `index.html` link to this exact
  filename. Export your résumé to PDF and save it with this name.
- **A headshot** (optional) — e.g. `headshot.jpg`. Keep it under 400 KB so the page stays fast.

If you would rather not publish a résumé PDF, delete the two résumé buttons from
`index.html` instead of leaving a broken link.
